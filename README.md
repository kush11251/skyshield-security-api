# SkyShield Security API
A robust security information and event management system. To run this project, execute the following command in your terminal:
```
git clone https://github.com/user/skyshield-security-api.git
npm install
cd src
node index.js
```
This will start the server and you can access it at http://localhost:3000